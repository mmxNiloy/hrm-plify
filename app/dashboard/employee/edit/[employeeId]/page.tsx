import React from "react";

interface Props {
  params: {
    employeeId: number;
  };
}

export default function EditEmployeeByIdPage({ params }: Props) {
  // Get employee full details here

  return <div>EditEmployeeByIdPage</div>;
}
